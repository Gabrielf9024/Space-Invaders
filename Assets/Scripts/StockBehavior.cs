using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StockBehavior : MonoBehaviour {

	// public GameObject StockPrefab;

	// private GameObject stock1;
	// private GameObject stock2;

	// // Use this for initialization
	// void Start () {
	// 	Vector3 spawn1 = new Vector3(-6.5f, -4.8f, 0f);
	// 	Vector3 spawn2 = new Vector3(-7f, -4.8f, 0f);

		// stock1 = Instantiate(StockPrefab, this.transform);
		// stock2 = Instantiate(StockPrefab, this.transform);
		// stock1.transform.localPosition = spawn1;
		// stock2.transform.localPosition = spawn2;
		// stock1.name = "stock1";
		// stock2.name = "stock2";
	// }
	
	// // Update is called once per frame
	// void Update () {
	// 	GameObject player = GameObject.Find("PlayerShip");
	// 	if( player.GetComponent<PlayerScript>().lives == 2 )
	// 		Destroy(stock2.gameObject);

	// 	if( player.GetComponent<PlayerScript>().lives == 1 )
	// 		Destroy(stock1.gameObject);
	// }
}
